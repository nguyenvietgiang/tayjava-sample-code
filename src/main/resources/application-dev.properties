server.port= 8000

# connect to Oracle
spring.datasource.url=jdbc:oracle:thin:@192.168.225.1:1521:ORCL
spring.datasource.username=C##JavaSpring
spring.datasource.password=strongpassword
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Hibernate DDL Auto
spring.jpa.hibernate.ddl-auto=update

# SQL config
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Swagger UI và API docs
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# Email config (must change when push to git)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=takadishen@gmail.com
spring.mail.password=do-not-comit-to-git
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# JWT Configuration
jwt.secret=your_secret_key_here
jwt.expiration.access=3600
jwt.expiration.refresh=86400
jwt.refresh.secret=your_refresh_secret_key_here

jwt.header=Authorization
jwt.prefix=Bearer
jwt.issuer=your_app_name
jwt.audience=your_app_audience

# redis config
spring.redis.host=localhost
spring.redis.port=6379




